export const iterativeStackTreeDfsNotes = {
  shortPatternNote:
    "Iterative preorder DFS uses an explicit stack: pop the current node, visit it, then push right before left so the left branch is processed next.",
}
